# The MEOS IDL is generated by MEOS-API and vendored in, not hand-edited.
codegen/input/meos-idl.json linguist-generated=true -diff

# The native MEOS shared library is built in CI, not committed; treat as binary.
*.so binary
